import { ComingSoon } from "@/components/ui/coming-soon";

export default function ClientsPage() {
  return (
    <ComingSoon
      title="Clients"
      phase="Phase 3"
      blurb="Client cards, detail views, milestones, communications, and auto-calculated health land in Phase 3."
    />
  );
}
