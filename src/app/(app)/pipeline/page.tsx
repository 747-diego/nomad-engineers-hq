import { ComingSoon } from "@/components/ui/coming-soon";

export default function PipelinePage() {
  return (
    <ComingSoon
      title="Pipeline"
      phase="Phase 3"
      blurb="Pipeline-only client stack with drag-to-reorder and one-tap convert to active arrives in Phase 3."
    />
  );
}
