import { StudioPulse } from "@/components/home/studio-pulse";

// Home / Studio Pulse (spec §5.2). The mandatory standup gate lives inside.
export default function HomePage() {
  return <StudioPulse />;
}
